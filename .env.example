# ChromaDB Configuration
CHROMA_PATH=./chroma-data

# Optional: Server Configuration
MCP_SERVER_NAME=my-mcp-server
MCP_SERVER_VERSION=1.0.0

# Optional: Embedding Model (if using external)
# OPENAI_API_KEY=your-api-key
